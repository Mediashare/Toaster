{% extends "base.html.twig" %}
{% block content %}
{% set more_results = (stockage.files|length - files|length) %}
    <h2 class="ui dividing header">
        <a href="{{ path('stockage_show', {'token': stockage.token}) }}">
            {{ stockage.name }}
        </a>
        /
    </h2>
    <div id="page" class="ui grid">
        <div class="ten wide column">
            {% if files|length %}
                <div class="ui one cards">
                    {% for file in files %}
                        {% include "file/_card.html.twig" %}
                    {% endfor %}
                </div>
            {% else %}
                <p>File(s) is not found.</p>
            {% endif %}
        </div>
        <div class="six wide column">
            <div class="ui sticky sticky_page" style="padding-top: 4rem;">
                {% if stockage.description %}
                    <h3 class="ui dividing header">
                        Description
                    </h3>
                    {% include "stockage/_description.html.twig" %}
                {% endif %}

                {% include "stockage/_actions.html.twig" %}

                <h3 class="ui dividing header">
                    Informations
                </h3>
                {% include "stockage/_info.html.twig" %}
                
                {% if stockage.tags %}
                    <h3 class="ui dividing header">
                        Tags
                    </h3>
                    {% include "stockage/_tags.html.twig" %}
                {% endif %}
            </div>
        </div>
    </div>
{% endblock content %}
