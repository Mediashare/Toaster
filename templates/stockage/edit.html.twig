{% extends "base.html.twig" %}
{% block content %}
    <h2 class="ui dividing header">
        <a href="{{ path('stockage_show', {'token': stockage.token}) }}">
            {{ stockage.name }}
        </a>
        / Edit
    </h2>

    <div id="page">
        <form method="POST" id="edit_stockage" class="ui form" style="margin-top: 1rem;">
            <div class="ui grid">
                <div class="ten wide column">
                    <div class="field">
                        <label for="edit_stockage_description">Stockage Description</label>
                        <div class="ui card w-100">
                            <textarea id="edit_stockage_description" name="edit_stockage_description" required="false">{{ stockage.description }}</textarea>
                        </div>
                    </div>
                    <script>
                        var textarea_description = CodeMirror.fromTextArea(document.getElementById("edit_stockage_description"), {
                            mode: {
                                name: "gfm",
                                tokenTypeOverrides: {
                                    emoji: "emoji"
                                }
                            },
                            scrollbarStyle: "simple",
                            lineNumbers: true,
                            theme: "default"
                        });
                        textarea_description.setSize("100%", "100%");
                    </script>
                </div>
                <div class="six wide column">
                    <div class="field">
                        <label>Name</label>
                        <div class="two fields">
                            <div class="field">
                                <input type="text" name="name" placeholder="Stockage name" value="{{ stockage.name }}" required>
                            </div>
                        </div>
                    </div>
                    <button onclick="$('form#edit_stockage').submit();" type="submit" class="ui button basic green">
                        <i class="edit icon"></i>
                        Edit
                    </button>
                </div>
            </div>
        </form>
    </div>
{% endblock content %}